# ARCHIVED — authichain-telegram

This worker is fully implemented (Telegram bot webhook handler with D1 + KV session state) but is not on the critical launch path. Deploy when the Telegram bot is actively needed for support/notifications.

**To deploy:** `wrangler deploy` from this directory after setting `BOT_TOKEN` secret.
