# Lean AgentZ FastAPI image for Cloudflare Containers.
# Boots /health + /agents (+ fleet routes). Heavy LLM / browser stacks are OPTIONAL —
# install requirements-agentz.txt on a full host if you need those workflows.
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
python-dotenv>=1.0
httpx>=0.27
# Soft deps used by api.main imports — keep minimal; missing optional modules
# should not prevent /health from booting (see agentz/api/container_app.py).
supabase>=2.4.0
PyYAML>=6.0
