# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.dist

# Generated files
*.tsbuildinfo
coverage/

# Package files
package-lock.json
pnpm-lock.yaml

# Database
*.db
*.sqlite
*.sqlite3

# Logs
*.log

# Environment files
.env*

# IDE files
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Prettier's TS parser stack-overflows on this file's single giant HTML
# template literal (RangeError: Maximum call stack size exceeded, reproduces
# on prettier 3.9.6 independent of any specific edit).
workers/authichain-com/src/index.ts

# Contains a base64 PNG assembled from ~1,900 concatenated string literals
# (OG_IMAGE_PNG_B64). Prettier's TypeScript parser overflows its stack on that
# expression — "RangeError: Maximum call stack size exceeded" — which fails the
# pre-commit hook for any change to this file. Formatting is skipped here until
# the image is moved out of source.
workers/strainchain-io/src/index.ts

# Two ~195k-character base64 JPEG literals (QR_ART, QR_BRAND) inlined as worker
# assets. Prettier's parser overflows its stack on these, same failure mode as
# the two files above. Skip until the images move out of source.
workers/passport-demo/src/index.js
