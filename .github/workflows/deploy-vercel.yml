<<<<<<< HEAD
name: Deploy Next.js to Vercel
=======
name: Deploy Vercel App (DISABLED — migrated to deploy-cf-pages.yml)
>>>>>>> origin/add-agentz-editable

# Trigger manually only; push-to-main now handled by deploy-cf-pages.yml
on:
<<<<<<< HEAD
  push:
    branches:
      - main
=======
>>>>>>> origin/add-agentz-editable
  workflow_dispatch:

permissions:
  contents: read
<<<<<<< HEAD

# Ensures that only one deployment runs at a time
concurrency:
  group: vercel-deploy
  cancel-in-progress: true
=======
>>>>>>> origin/add-agentz-editable

env:
  # Required by Vercel CLI to link the project
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
  NODE_VERSION: '22'

jobs:
<<<<<<< HEAD
  deploy-production:
    name: Autonomous Vercel Production Deployment
    runs-on: ubuntu-latest
    timeout-minutes: 15

=======
  deploy-worker:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        project:
          - workers/qron-automation
    name: deploy (${{ matrix.project }})
>>>>>>> origin/add-agentz-editable
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

<<<<<<< HEAD
      - name: Install Vercel CLI
        run: pnpm add -g vercel@latest

      - name: Pull Vercel Environment Information
        # Fetches the linked project's production environment variables
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build Project Artifacts
        # Uses Next.js to compile the production build locally in the runner
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy to Vercel
        # Pushes the prebuilt artifacts to Vercel for immediate deployment
        run: vercel deploy --prebuilt --prod --archive=tgz --token=${{ secrets.VERCEL_TOKEN }}
=======
      - name: Deploy Cloudflare Worker
        run: npx wrangler deploy --minify --config wrangler.toml
        working-directory: ${{ matrix.project }}
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
>>>>>>> origin/add-agentz-editable
