name: CI Main

# Required quality gate: install, typecheck, lint, test, production build.
# Schema/AgentZ extras live in ci.yml so this job is not duplicated there.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    name: Main quality gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: "10.28.2"

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Validate secrets format
        run: node scripts/check-secrets-format.mjs
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile --config.minimumReleaseAge=0

      - name: Type check
        run: pnpm check

      - name: Lint
        run: pnpm lint:ci

      - name: Install Playwright browsers
        run: pnpm --filter agent-browser exec playwright install --with-deps chromium

      # `pnpm test` runs `turbo test`, and turbo.json's test task
      # `dependsOn: ["build"]` builds every workspace app first — including
      # every Next.js app using next/font/google, not just the root app the
      # "Production build" step below covers. That step already retries once
      # for the same flake (see its comment); this implicit build had no such
      # retry, so a font-fetch hiccup here failed the whole required gate with
      # no test output at all. Second `pnpm test` keeps the step's exit code —
      # do not `set +e`.
      #
      # `--continue=dependencies-successful` keeps turbo running every other
      # package's tests after one fails, so a single red package no longer
      # hides failures elsewhere; turbo still exits non-zero if any task
      # failed. A package whose own build failed is skipped, not tested.
      - name: Run tests
        run: |
          if pnpm test --continue=dependencies-successful; then
            exit 0
          fi
          echo "Tests failed; retrying once for a possible next/font fetch flake in a workspace build"
          sleep 15
          pnpm test --continue=dependencies-successful

      # next/font/google sometimes 404s mid-build (null CSS parse at
      # google/loader.js:122 on Public_Sans). Same layout just passed on
      # sibling PRs; one retry is cheaper than a red required gate.
      # Second `pnpm build` keeps the step's exit code — do not `set +e`.
      - name: Production build
        run: |
          if pnpm build; then
            exit 0
          fi
          echo "Production build failed; retrying once for next/font fetch flake"
          sleep 15
          pnpm build

  production-proof:
    name: Production launch proof
    if: github.event_name == 'push' && contains(github.event.head_commit.message, '[pipeline-proof]')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: "10.28.2"

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile --config.minimumReleaseAge=0

      - name: Run autonomous revenue pipeline
        env:
          AUTONOMOUS_PIPELINE_ENABLED: ${{ vars.PIPELINE_TICK_ENABLED != 'false' && 'true' || 'false' }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          APOLLO_API_KEY: ${{ secrets.APOLLO_API_KEY }}
          HUBSPOT_TOKEN: ${{ secrets.HUBSPOT_TOKEN }}
          HUBSPOT_ACCESS_TOKEN: ${{ secrets.HUBSPOT_ACCESS_TOKEN }}
          NODE_OPTIONS: --dns-result-order=ipv4first
        run: pnpm tsx server/jobs/pipeline-tick.ts

      - name: Produce cryptographic QRON + StoryMode proof
        if: always()
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          AUTHICHAIN_ATTESTATION_KEY_ID: ${{ secrets.AUTHICHAIN_ATTESTATION_KEY_ID }}
          AUTHICHAIN_ATTESTATION_PRIVATE_KEY_B64: ${{ secrets.AUTHICHAIN_ATTESTATION_PRIVATE_KEY_B64 }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_RUN_ID: ${{ github.run_id }}
        run: pnpm tsx scripts/launch-proof.ts

      - name: Upload launch proof
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: authichain-production-launch-proof
          path: |
            artifacts/production-launch-proof.json
            artifacts/launch-proof-qron.png
          if-no-files-found: warn
