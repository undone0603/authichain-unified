# Lean first-boot set for the Cloudflare Containers image.
# /health must import; /agents needs ALL_AGENTS (pydantic + agentz.agents).
# credentials.get() needs python-dotenv + httpx. /workflows needs PyYAML.
fastapi>=0.100.0
uvicorn>=0.22.0
supabase>=2.4.0
pydantic>=2.0.0
python-dotenv>=1.0
httpx>=0.27
PyYAML>=6.0
