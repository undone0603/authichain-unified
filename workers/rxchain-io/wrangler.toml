name = "rxchain-io"
main = "src/index.ts"
compatibility_date = "2026-04-01"
account_id = "4c1869b90f13f86940aa3747839bf420"
workers_dev = true
# Custom-domain routes disabled 2026-09-08: the rxchain.io zone does not exist
# in the Cloudflare account, so wrangler deploy failed with
# "Could not find zone for rxchain.io". Restore the routes block below once the
# zone is re-registered:
# [[routes]]
# pattern = "rxchain.io/*"
# zone_name = "rxchain.io"
#
# [[routes]]
# pattern = "www.rxchain.io/*"
# zone_name = "rxchain.io"
