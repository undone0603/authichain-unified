{
  "name": "gs1-resolver",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute authichain-provenance --file=src/schema.sql",
    "test": "echo \"gs1-resolver: no tests yet (src/index.ts entrypoint also not yet implemented \u2014 wrangler.toml points at it)\" && exit 0"
  }
}
