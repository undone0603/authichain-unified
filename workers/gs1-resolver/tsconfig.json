{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "strict": true,
    "types": ["@cloudflare/workers-types"],
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src/**/*.ts"],
  "// exclude": [
    "*.test.ts runs under Node (node --experimental-strip-types), not the Workers",
    "runtime. Typechecking it here would need @types/node and",
    "allowImportingTsExtensions in a config that should stay Worker-only, and",
    "pulling node types in would let worker code reference Node globals that do",
    "not exist at runtime. The tests are verified by executing them: pnpm test."
  ],
  "exclude": ["src/**/*.test.ts"]
}
