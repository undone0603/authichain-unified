# Flip list — gated / stubbed / dry-run money paths

**Audited:** 2026-09-20 from `main` @ `c1d80972` plus GitHub Actions API (workflow enable-state, recent run logs).  
**Constraint:** $0 spend. No Workers Paid / Containers. No invented Stripe prices. No live charges without naming buyer + amount. Grok Bot must not post on X.  
**Not a deploy.** This file is the ranked list; do not treat it as a thaw of `content-publish` or `gov-*`.

Execute from this list + live Actions state. `docs/operations/PUBLIC_LOOP_FREEZE.md` tables still lag GitHub.

---

## Already flipped — do not re-recommend

| Item | Evidence | What actually happened |
| --- | --- | --- |
| P0–P2 estate pricing | #1089 `5920b569`, #1095 `02d0da5d` | Apex `/pricing` on authichain.com / qron.space / strainchain.io serves live catalogue or StrainChain Basic Payment Link. |
| StrainChain Basic $199/mo | `PAYMENT_LINKS.strainchain.basic` + worker HTML | Live Stripe Payment Link is the only StrainChain checkout. Passport/Farm SKUs stay `stripe_price_id: null`. |
| Ghost money-path probes + IndexNow money URLs | #1097 `c1d80972` | `ghost_traffic_engine.py` GETs `/pricing` `/dpp` `/x402` `/onboard` + DPP 303. `marketing-autonomous.yml` IndexNow list includes those URLs. |
| License D1 0002 **workflow** | #1094 `58221869` `.github/workflows/d1-license-migrate.yml` | Dispatch exists. **Apply failed** (see Blocked). Not live on D1. |
| Channel partners live | #1071, #1077, #1080; runs `35483949929` and `35502804722` | `OWNER_LIVE_SEND=true`, `ALLOW_PARTNER_SENDS=true`, `MAX_LIVE_SENDS=2`. **Sent twice the same day:** `contact@existosolutions.com` and `info@icsconsultingservice.com`. NEMC APEX still queued. Do **not** re-dispatch `segment=partners`. |
| x402 rail | #1068, #1072–#1078; health `ready` / `trustless` | Public `/x402`, unpaid POST 402, PayTo bound. Do not rebind. |
| DPP checkout + webhook harden | #1079, #1084, #1085, #1088 | `GET /api/checkout/dpp` 303. Edge webhook persist + guest `provisioned`. |
| `POST /api/generate` credits | #1090 | Paid credit spend is callable on the edge, not ASSETS 404. |
| `OWNER_LIVE_SEND=true` | Workflow logs 2026-09-20: `Manual live send (OWNER_LIVE_SEND=true)` | Armed. Monday `b2b-outreach` schedule + weekday `email-proposals` schedule will go **live** unless disarmed. |
| Workflows active | Actions API 2026-09-20 | `b2b-outreach`, `email-proposals`, `agentz-orchestration`, `gov-mint`, `marketing-autonomous`, `content-routine-pr`, `ghost-traffic`, `genesis-cron` are **active**. |
| Social bundle on main | #1092 `content/social/2026-09-20-consumer-verify-flow-a.json` | Validated and merged. **Not posted** — `content-publish.yml` is disabled and `.published.json` is `{}`. |

---

## Risk already armed (not a flip)

Monday 14:00 UTC `b2b-outreach.yml` schedule uses `inputs.segment \|\| 'all'` and, with `OWNER_LIVE_SEND=true`, sets `dry_run=false`. That is a live `all` batch (govchain + strainchain + qron), still capped at `MAX_LIVE_SENDS=2`, **not** partners. Weekday 15:00 UTC `email-proposals.yml` likewise goes live.

If that is not intended: `gh variable set OWNER_LIVE_SEND --body false -R undone0603/authichain-unified` before Monday.

Partner idempotency was weak: two live partner dispatches both Resend-sent Existo and ICS. `PARTNER_ALREADY_SENT` in `scripts/lib/b2b-send-policy.ts` now hard-blocks those two desks (plus case-insensitive `contacted` lookup). Do not re-dispatch `segment=partners` until that change is on `main`.

---

## Ready to flip now

| Flip | Evidence | Current state | What flipping does | Money / traffic link | Risk / cost | Priority | Recommended next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **QRON live tiny batch** | `.github/workflows/b2b-outreach.yml` L5–6, L129; freeze doc QRON addresses `franchiseinfo@fastsigns.com`, `inquiries@moo.com`; `scripts/b2b-cold-outreach.ts` Creator Pack CTA `buy.stripe.com/28E00l6OT7dHcjI1MgaIM0d` | Workflow **active**, `OWNER_LIVE_SEND` true, QRON not yet live-dispatched (today’s QRON/strainchain dispatches were dry-run) | Sends at most **2** Resend emails in the QRON segment with the live Creator Pack Payment Link | Direct pack revenue ($99 Creator / $29 Starter already on `/pricing`) | $0 infra. Blast = 2 named inboxes. Needs Resend (already used for partners). Do **not** use `segment=all`. | **P0** | `gh workflow run b2b-outreach.yml -R undone0603/authichain-unified -f segment=qron -f dry_run=false` |
| **IndexNow money URLs — fire once on current main** | #1097 `marketing-autonomous.yml` pings `/pricing` `/dpp` `/x402` `/onboard` + estate CTAs; last SEO dispatch **2026-09-20 09:13Z** (likely **before** `c1d80972` landed) | Code on `main`; scheduled SEO is Friday 09:00 UTC only | Asks IndexNow to fetch the live checkout/onboard URLs now instead of waiting until Friday | Traffic into DPP $299, StrainChain Basic, QRON packs, x402 docs | $0. Fail-soft per URL. No social post. No X. | **P0** | `gh workflow run marketing-autonomous.yml -R undone0603/authichain-unified -f strategy=seo_only -f dry_run=true` |
| **DPP exception report (observability)** | `.github/workflows/autonomous-business-cycle.yml` dispatch-only; `GET /api/cron/dpp-exceptions` mounted on edge; schedule retired 2026-09-08 | Partial — handler live, no daily tick | Prints stall counts (`checkout_started` vs `payment_succeeded` / `provisioned`) so the next real dollar is visible | Does not charge. Prevents silent fulfill gaps on DPP. | $0. Needs `CRON_SECRET` match (genesis already uses it). Read-only GET. | **P1** | `gh workflow run autonomous-business-cycle.yml -R undone0603/authichain-unified -f task=dpp-exceptions -f live=true` |
| **Email proposals — one live weekday batch** | `email-proposals.yml` schedule 15:00 UTC Mon–Fri; `scripts/email-proposals.ts` DPP Payment Link `bJe7sLgDTaRwh0S9vu1ND0c`; last schedule 2026-09-15; last dispatch 2026-09-19 | Active; live iff `OWNER_LIVE_SEND` (already true). Next schedule is **Monday**. | Sends unsent `gov_proposals` with fit ≥ 70, each carrying the live $299 DPP link | Gov → DPP checkout | $0 infra. Blast = whatever is `email_status=unsent` (not capped at 2). Review one dry-run log first if Monday auto-send is too wide. | **P1** | Dry first: `gh workflow run email-proposals.yml -f dry_run=true`. Live: `-f dry_run=false` **or** leave Monday schedule. |

---

## Needs a small code unlock

| Flip | Evidence | Current state | What flipping does | Money / traffic link | Risk / cost | Priority | Recommended next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **Apex sitemap money URLs** | `workers/authichain-com/src/index.ts` ~L3174–3188 lists `/`, `/pricing`, `/anchor`, `/protocol`, `/digital-product-passport`, `/x402`, `/contact` — **not** `/dpp`, `/onboard`, `/verify`. IndexNow already pings `/dpp` `/onboard`. `/dpp` is an alias of `/digital-product-passport`. | Partial | Search engines that trust `sitemap.xml` discover checkout + intake + verify | Organic traffic to DPP / onboard / verify | $0. One worker sitemap array edit + test. Redeploy `authichain-com` only. | **P0** | Add `{ loc: 'https://authichain.com/dpp' }`, `/onboard`, `/verify` to `staticUrls`. Deploy `worker=authichain-com`. |
| **QRON credit Payment Links on `/generate`** | `server/payment-links.ts` `credits50` $9.99, `credits250` $39.99, `credits1000` $99.99 — **zero** references outside that file + revenue-cycle dump. #1090 made `POST /api/generate` spend credits. Estate `/pricing` shows Starter $29 / Creator $99 packs only. | Off (links exist, unused) | Surfaces already-created Stripe Payment Links next to generation | Impulse pack/credit revenue on the generate path | $0. Do not invent prices — reuse `PAYMENT_LINKS.qron.credits*`. Keep packs vs credits copy distinct. | **P1** | Add a credits row on `qron.space` generate/pricing HTML from `PAYMENT_LINKS.qron`, no new Stripe objects. |
| **AuthiChain Starter $299/mo Payment Link** | `PAYMENT_LINKS.authichain.starter` `buy.stripe.com/28E8wP0EVf7M6mefTS1Nu1p`. Estate `/pricing` uses `listedPlans("qron")` from `src/lib/plans.ts` — that $299 link is **not** in `PLANS`. | Off (link live, not on apex) | Puts the existing AuthiChain subscription link on authichain.com `/pricing` | Recurring AuthiChain revenue alongside DPP one-time | $0 if the Stripe link is still active. Naming collision with QRON “Starter Pack” $29 — label must stay “AuthiChain Starter $299/mo”. Do not invent a price id. | **P2** | Confirm the Payment Link is still open in Stripe Dashboard, then add one card on origin=`authichain` only. |
| **`retained` auto-write** | `src/lib/dpp-loop.ts` `evaluateRetention` (L446+) used only by `dpp-smoke-e2e.test.ts`. Funnel accepts alias `retained` (#1075) but nothing scheduled writes it. | Stub / test-only | After activation + horizon, records `retained` so the loop metric can go green | Retention signal for DPP conversion, not first dollar | $0. Wire into `GET /api/cron/dpp-exceptions` (already mounted). No new secrets. | **P2** | In the exceptions cron, call `evaluateRetention` and `recordFunnelEvent(..., stage: "retained")` when true. |
| **Edge `POST /api/dpp/publish` + `/api/dpp/verify`** | Next routes exist (`src/app/api/dpp/publish/route.ts`, `verify/route.ts`). Not in `worker-app/`. Funnel aliases do **not** insert a `products` row. | Partial | A paid merchant can publish/verify a DPP on the same origin as checkout | Completes the DPP loop after provision | $0. Medium blast (product writes). Needs Supabase on edge (already for webhook). | **P1** | Port the two Next handlers onto `worker-app` the same way `/api/funnel` was mounted. |
| **govchain.us `/pricing`** | `workers/govchain-us/src/routing.test.ts` L64 treats `/pricing` as **404**. Live sitemap correctly omits it. Stale `public/sitemap-govchain-us.xml` still lists it. Homepage CTA is AuthiChain pricing. | Off | Optional: serve DPP checkout CTA on govchain `/pricing` (estate helper already exists) | Gov traffic → $299 DPP | $0. Must not invent GovChain SKUs. | **P2** | Either `tryHandleEstatePricing(request, "authichain")` on govchain.us **or** leave 404 and delete the stale public sitemap URL. |
| **Ordinals Payment Links on apex** | `src/app/pricing/page.tsx` L146–171 three `buy.stripe.com` links ($49 / $299 / $799). Apex estate pricing does not render that page. | Off on public apex | Would show Bitcoin inscription SKUs on qron/authichain `/pricing` | Secondary SKUs; not DPP/packs | $0 only if those links are still live. Easy to confuse with QRON packs. | **P2** | Confirm Stripe Dashboard; if live, add an “Ordinals” subsection on origin=`qron` only. |

---

## Blocked on owner / secret / paid plan

| Flip | Evidence | Current state | What flipping does | Money / traffic link | Risk / cost | Priority | Recommended next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **License D1 0002 apply** | Migration `workers/authichain-license-issuer/migrations/0002_one_active_license_per_customer.sql`. Run [35501621061](https://github.com/undone0603/authichain-unified/actions/runs/35501621061) **failure**: Cloudflare API `code: 7403` “account is not valid or is not authorized” on `authichain-license-db`. | Stubbed uniqueness in SQL; **not applied** | Unique active license per Stripe customer — required before license revenue can retry-issue safely | Agent-browser / license SKUs | $0 once token has D1 edit. Owner must widen `CLOUDFLARE_API_TOKEN` (D1). Not Workers Paid. | **P1** | Grant D1 on the existing token, then `gh workflow run d1-license-migrate.yml`. Do not guess-apply. |
| **GSC sitemap PUT** | `marketing-autonomous.yml` L81–86: skip if `GSC_SERVICE_ACCOUNT_JSON` unset. Social-credentials pattern: empty secrets fail-soft. | Dry / skipped | Submits estate sitemaps to Google Search Console | Indexing of `/pricing` `/dpp` `/x402` | $0 API. Owner JSON key in repo secrets. | **P1** | Owner: add `GSC_SERVICE_ACCOUNT_JSON`, then same `seo_only` dispatch as IndexNow. |
| **content-publish (LinkedIn / Reddit / Telegram)** | Workflow **disabled_manually** (`332964370`). Schedule is fail-closed dry-run (#1075). `content/social/.published.json` = `{}`. 2026-09-14 `social-credentials-check`: **all five channels unconfigured** (LinkedIn, Reddit, Twitter, Telegram, webhook). Bundle `consumer-verify-flow-a` sits unpublished. | Frozen + secrets missing | Would post the oldest validated bundle. Twitter/X secrets are **unset**, so enabling would **not** tweet (Grok Bot / X constraint satisfied). | Traffic to `/verify` (bundle UTM) | $0 if Telegram only (BotFather). LinkedIn/Reddit need OAuth. Do **not** set `TWITTER_*`. Do **not** enable until at least Telegram **or** webhook exists — otherwise the job is a no-op. | **P1** | 1) Owner: `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHANNEL_ID` via `set-social-secrets.yml`. 2) `gh api -X PUT repos/undone0603/authichain-unified/actions/workflows/332964370/enable`. 3) `gh workflow run content-publish.yml -f dry_run=true` then `-f dry_run=false -f bundle=content/social/2026-09-20-consumer-verify-flow-a.json`. |
| **License email delivery** | `workers/authichain-license-issuer/src/index.ts` `RESEND_API_KEY?` — issues licenses without mailing if unset | Partial | Emails the issued key to the paying customer | License fulfillment | $0. Owner Worker secret. | **P2** | `wrangler secret put RESEND_API_KEY` on `authichain-license-issuer` after D1 0002 applies. |
| **high_leverage live** | `scripts/lib/high-leverage.ts` refuses live; YAML: “do not invent a live flag” | Dry-run only by design | Would email five named Tier 1 inboxes | StrainChain / QRON / Curaleaf / C3 | Owner decision. Cap would need code. Not a $0 flip today. | **P2** | Keep dry-run. `gh workflow run b2b-outreach.yml -f segment=high_leverage -f dry_run=true` only. |
| **Sibling `gov-*` + live mint** | `gov-ingest/score/proposals/notify/engine` **disabled_manually**. `gov-mint` active, default `dry_run=true`. Item 5: Base `getCode=0x` until deploy. | Frozen | SAM ingest → score → proposal → mint | Gov pipeline, not first Stripe dollar | Needs bytecode on 8453 (`deploy-govchain-nft-base.yml` `dry_run=false`) + owner secret `GOVCHAIN_NFT_CONTRACT`. | **P2** | Leave off. After bytecode: ingest → score → proposals → mint dry-run → notify → engine. |
| **weekly-video** | Disabled. Requires `HEYGEN_API_KEY` (paid render) + YouTube OAuth | Frozen | Weekly YouTube → qron.space RSS | QRON traffic | **Not $0** (HeyGen). | — | Do not enable under this budget. |
| **GROUP B wrangler crons** | `worker-app/wrangler.toml` commented; includes founder-payout (moves money), outreach, METRC | Frozen | Autonomous HubSpot/outreach/on-chain | High blast | Cloudflare free cron cap + spend. | — | Do not uncomment. |
| **Workers Paid / Containers / AgentZ Containers** | #1057 in-repo; freeze + backlog | Off | Host AgentZ on CF Containers | None until revenue | **Paid plan.** Stay on $0 Tunnel. | — | Do not enable. |
| **qron-outreach `/send-next`** | `outreach-trigger.yml` schedule commented 2026-08-07; queues exhausted (CAPABILITIES) | Dormant | Would send next queued QRON/DPP worker email | Empty queue = no-op | Re-enable only after queue refill + bounce fix. | — | Do not flip. |
| **NEMC APEX next partner pair** | Queued after Existo/ICS cap: `fitzpatricks@` / `mooret@` / `mcmanuss@nemcworks.org` | Queued | Next `MAX_LIVE_SENDS=2` partner emails | Channel partners | **Unblocked in code** — Existo/ICS are on `PARTNER_ALREADY_SENT`. Merge first. | **P1** | After this lands on `main`: `-f segment=partners -f dry_run=true`, then `-f dry_run=false`. Do not flush partners with the cold queue. |

---

## Disabled workflows (not money)

Leave disabled: `dependabot-auto-merge`, `automerge-dependabot`, `browser-vision-tasks`, `repo-maintenance`, `weekly-video`, `content-publish` (until Telegram/webhook), all `gov-*` except mint dry-run.

`CLOUDFLARE_DEPLOY_ENABLED` still gates scheduled `deploy-cloudflare.yml`; dispatch bypasses it (steward skill).

---

## Top 3 next (under $0, low blast)

1. **QRON live tiny batch (P0)** — `OWNER_LIVE_SEND` is already true; cap is 2; CTA is the live Creator Pack link.  
   `gh workflow run b2b-outreach.yml -R undone0603/authichain-unified -f segment=qron -f dry_run=false`

2. **IndexNow now (P0)** — money URLs are in #1097; do not wait for Friday.  
   `gh workflow run marketing-autonomous.yml -R undone0603/authichain-unified -f strategy=seo_only -f dry_run=true`

3. **Apex sitemap `/dpp` `/onboard` `/verify` (P0 code)** — IndexNow already advertises them; `sitemap.xml` does not. Smallest traffic unlock that is not a secret.

**Do not:** re-dispatch partners; enable `content-publish` with empty social secrets; set Twitter keys; uncomment GROUP B; enable HeyGen; rebind x402; invent StrainChain passport prices; treat D1 0002 as applied.
